#language used
language: cpp

#compiler
compiler: gcc

#install:
install:
 - sudo apt-get install binutils-mingw-w64

#build
script: 
 - make all

after_success:
 - echo "Build effettuata con successo" 
